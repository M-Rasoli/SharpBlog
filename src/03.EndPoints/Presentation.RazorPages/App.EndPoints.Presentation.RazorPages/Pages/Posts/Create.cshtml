@page
@model App.EndPoints.Presentation.RazorPages.Pages.Posts.CreateModel
@{
    ViewData["Title"] = "پست جدید";
    ViewData["PageTitle"] = "پست جدید";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<main class="max-w-4xl mx-auto px-4 pt-6 pb-20">
    <div class="bg-white rounded-3xl shadow-xl overflow-hidden">
        <div class="bg-gradient-to-r from-indigo-500 to-purple-600 p-6 text-white">
            <h1 class="text-2xl font-bold text-center">ایجاد پست جدید</h1>
        </div>

        <form method="post" enctype="multipart/form-data" class="p-6 space-y-8">
            <!-- ناحیه آپلود + پیش‌نمایش -->
            <div class="grid lg:grid-cols-2 gap-8">
                <!-- سمت چپ: آپلود و پیش‌نمایش -->
                <div class="space-y-6">
                    <div id="uploadArea" class="border-4 border-dashed border-gray-300 rounded-2xl p-10 text-center cursor-pointer hover:border-indigo-400 transition"
                         onclick="document.getElementById('fileInput').click()">
                        <input type="file" id="fileInput" asp-for="Model.Image" accept="image/jpeg,image/jpg,image/png" class="hidden" onchange="handleFileSelect(event)">

                        <div id="uploadContent">
                            <i data-lucide="image" class="w-16 h-16 text-gray-400 mx-auto mb-4"></i>
                            <p class="text-lg font-medium text-gray-700">عکس را اینجا بکشید یا کلیک کنید</p>
                            <p class="text-sm text-gray-500 mt-2">
                                فرمت‌های مجاز: JPG ،PNG — حداکثر حجم فایل: ۲ مگابایت
                            </p>
                        </div>

                        <!-- پیش‌نمایش تصویر -->
                        <div id="imagePreview" class="hidden">
                            <img id="previewImg" src="" alt="پیش‌نمایش" class="w-full rounded-xl shadow-lg">
                            <button type="button" onclick="resetUpload()" class="mt-4 text-red-500 hover:text-red-700 text-sm">
                                تغییر عکس
                            </button>
                        </div>

                    </div>

                </div>
                <!-- سمت راست: فرم -->
                <div class="space-y-7">

                    <!-- عنوان پست -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            عنوان پست <span class="text-red-500">*</span>
                        </label>
                        <input asp-for="Model.Title" 
                               type="text" 
                               required
                               class="w-full px-5 py-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent text-lg font-medium placeholder-gray-400"
                               placeholder="عنوان را وارد کنید" />
                        <span asp-validation-for="Model.Title" class="text-red-500 text-xs mt-1 block"></span>
                    </div>

                    <!-- کپشن -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            متن اصلی 
                        </label>
                        <textarea asp-for="Model.Text" 
                  rows="6"
                  class="w-full px-5 py-4 border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent resize-none text-base"
                  placeholder=""></textarea>
                        <span asp-validation-for="Model.Text" class="text-red-500 text-xs mt-1 block"></span>
                    </div>

                    <!-- دسته‌بندی -->
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">دسته‌بندی</label>
                        <select asp-for="Model.CategoryId" 
                                required 
                                class="w-full px-5 py-4 text-base border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-transparent bg-white">
                            <option value="">انتخاب دسته‌بندی</option>
                            @foreach (var category in Model.Categories)
                            {
                                <option value="@category.Id">@category.Title</option>
                            }
                        </select>
                    </div>
                    @if (!string.IsNullOrEmpty(Model.Message))
                    {
                        <div class="mt-4 text-center text-sm text-red-600">@Model.Message</div>
                    }
                    <div class="flex justify-end gap-4 pt-6 border-t border-gray-200">
                      
                        <button type="submit"
                                class="px-10 py-3 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-bold rounded-full shadow-lg hover:shadow-xl transform hover:scale-105 transition flex items-center gap-2">
                             ثبت
                            <i data-lucide="send" class="w-5 h-5"></i>
                        </button>
                    </div>
                </div> <!-- پایان space-y-7 -->
            </div>
        </form>
    </div>
</main>

<!-- الرت سفارشی -->
<div id="customAlert" class="hidden fixed bottom-6 left-1/2 -translate-x-1/2 bg-gray-900 text-white px-6 py-3 rounded-full shadow-2xl z-50">
    <span id="alertMessage"></span>
</div>

@section Scripts {
    <script>
        let selectedFile = null;
        let selectedColor = null;
        const tags = [];

        function handleFileSelect(e) {
            const file = e.target.files[0];
            if (!file) return;

            selectedFile = file;
            const reader = new FileReader();
            reader.onload = function(ev) {
                document.getElementById('previewImg').src = ev.target.result;
                document.getElementById('imagePreview').classList.remove('hidden');
                document.getElementById('uploadContent').classList.add('hidden');
                document.getElementById('colorPreview').classList.add('hidden');
                selectedColor = null;
            };
            reader.readAsDataURL(file);
        }

        function selectColor(color) {
            selectedColor = color;
            document.getElementById('colorPreview').style.backgroundColor = color;
            document.getElementById('colorPreview').classList.remove('hidden');
            document.getElementById('imagePreview').classList.add('hidden');
            document.getElementById('uploadContent').classList.add('hidden');
            document.getElementById('fileInput').value = '';
        }

        function resetUpload() {
            document.getElementById('fileInput').value = '';
            document.getElementById('imagePreview').classList.add('hidden');
            document.getElementById('colorPreview').classList.add('hidden');
            document.getElementById('uploadContent').classList.remove('hidden');
            selectedFile = null;
            selectedColor = null;
        }

        function handleTagKeydown(e) {
            if (e.key === 'Enter' || e.key === ' ') {
                e.preventDefault();
                const value = e.target.value.trim();
                if (value && !tags.includes(value) && tags.length < 10) {
                    tags.push(value);
                    addTagPill(value);
                    e.target.value = '';
                }
            }
        }


        // نمایش پیام
        function showAlert(message, isError = false) {
            const alert = document.getElementById('customAlert');
            const msg = document.getElementById('alertMessage');
            msg.textContent = message;
            alert.className = `fixed bottom-6 left-1/2 -translate-x-1/2 px-6 py-3 rounded-full shadow-2xl z-50 text-white ${isError ? 'bg-red-600' : 'bg-green-600'}`;
            alert.classList.remove('hidden');
            setTimeout(() => alert.classList.add('hidden'), 3000);
        }


        lucide.createIcons();
    </script>
}
